#include <iostream>
using namespace std;
int main()
{
    int i=1,n;
    cin>>n;
    
    while(i<=n){
        int j=1;
        int count=n-i;
        while(count){
            cout<<"*";
            count--;
        }
        cout<<endl;
        i=i+1;
    }
    return 0;
}
